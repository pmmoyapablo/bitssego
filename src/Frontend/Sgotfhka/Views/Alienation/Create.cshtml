@model Sisgtfhka.Models.AlienationModel
@inject Microsoft.Extensions.Options.IOptions<AppSettings> Settings

@{
    ViewData["Title"] = "Crear Nueva Enajenación";
}

<h2>Crear Nueva Enajenación</h2>

<h4>Use el siguiente formulario para registrar una Enajenación</h4>
<hr />

<form method="post" class="form-inline" action="FindFinalClientRif">
    <div>
        Rif Cliente Final: <input asp-for="FinalClient.Rif" class="form-control" maxlength="10" onkeyup="toUpperChars(this)" width="80" />
        <span asp-validation-for="FinalClientId" class="text-danger"></span>
        <input type="submit" value="Buscar" class="btn btn-info" />
    </div>
</form>
<hr />
@{
    if (ViewData["Rif_New"] != null)
    {
        string urlbox = Settings.Value.UrlBox_RIF + "=" + ViewData["Rif_New"];

        <iframe width='60%' height='25%' src='@urlbox'></iframe>
    }
}
<hr />

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FinalClientId" class="control-label"></label>
                <input asp-for="FinalClient.Description" class="form-control" id="finalClientDescription" name="finalClientDescription" disabled="true" />
                <input asp-for="FinalClientId" class="form-control" type="hidden" />
                <span asp-validation-for="FinalClientId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Serial" class="control-label"></label>
                <input asp-for="Serial" class="form-control" id="Serial" name="Serial" maxlength="10" onkeyup="toUpperChars(this)"/>
                <span asp-validation-for="Serial" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Observations" class="control-label"></label>
                <input asp-for="Observations" class="form-control" />
                <span asp-validation-for="Observations" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AlienationDate" class="control-label"></label>
                <input asp-for="AlienationDate" class="form-control" />
                <span asp-validation-for="AlienationDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input asp-for="DistributorId" class="form-control" type="hidden" />
                <input type="submit" value="Crear" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Regresar</a>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script type="text/javascript">


    /*
        $("#Serial").change(function () {

            var val1 = $('input[name=Serial]').val();
            alert("Hola en el metodo: " + val1);


                $.ajax({
                    type: "POST",*/
                    //url: "@Url.Action("buscarSerial")",
                    /*data: {serial: val1},
                    dataType: "json",
                    success: function (msg) {
                        console.log(msg);
                        alert(msg.provider.description);
                        if (msg != null) {
                            $('input[name=Provider]').val(msg.provider.description);
                            $('input[name=Distributor]').val(msg.distributor.description);
                        } else {

                            $('input[name=Provider]').val("");
                            $('input[name=Distributor]').val("");
                        }
                    },
                    error: function (req, status, error) {
                        alert("Error");
                        $('input[name=Provider]').val("");
                        $('input[name=Distributor]').val("");
                    }



    });
    });

    */
</script>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        function toUpperChars(e) {
            e.value = e.value.toUpperCase();
        }
    </script>
}